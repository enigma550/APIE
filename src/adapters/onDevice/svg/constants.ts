import { join } from "node:path";

export const ICON_SIZE = 432;
export const DEX_PATH = join(import.meta.dir, "../../../../on-device/icon_extractor.dex");
export const REMOTE_DEX_PATH = "/data/local/tmp/icon_extractor.dex";
